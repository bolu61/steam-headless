# Part 2/2 of the Remote Play reconnect-deadlock workaround; see
# docs/remote-play-reconnect-deadlock.md. Companion:
# config/pipewire/pipewire-pulse.conf.d/50-headless-fallback-audio.conf

wireplumber.settings = {
  node.restore-default-targets = false
}
